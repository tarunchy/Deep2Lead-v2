# IBM MAMMAL service requirements
# Deploy on dlyog03 — RTX 3060 Ti, CUDA 12.4, Python 3.11

# PyTorch — install CUDA 12.1 wheel (binary compatible with CUDA 12.4 driver)
# Installed separately in setup.sh via: pip install torch --index-url https://download.pytorch.org/whl/cu121

# IBM MAMMAL framework
# Installed separately in setup.sh via: pip install git+https://github.com/BiomedSciAI/biomed-multi-alignment.git

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Utilities
requests>=2.31.0
tqdm>=4.66.0
